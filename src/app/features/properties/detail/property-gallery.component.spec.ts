import {TestBed} from '@angular/core/testing';import {PropertyGalleryComponent} from './property-gallery.component';
describe('PropertyGalleryComponent',()=>{const images=[{id:'1',image:'/one.jpg',caption:'Front',sort_order:0,is_cover:true,created_at:''},{id:'2',image:'/two.jpg',caption:'Kitchen',sort_order:1,is_cover:false,created_at:''}];it('renders multiple images, thumbnails and changes selection',()=>{const f=TestBed.createComponent(PropertyGalleryComponent);f.componentRef.setInput('title','Home');f.componentRef.setInput('images',images);f.detectChanges();expect(f.nativeElement.querySelectorAll('swiper-slide').length).toBe(2);expect(f.nativeElement.querySelectorAll('.thumbs button').length).toBe(2);f.componentInstance.select(1);expect(f.componentInstance.selected()).toBe(1)});it('hides thumbnails for one image',()=>{const f=TestBed.createComponent(PropertyGalleryComponent);f.componentRef.setInput('title','Home');f.componentRef.setInput('images',images.slice(0,1));f.detectChanges();expect(f.nativeElement.querySelector('.thumbs')).toBeNull()});it('renders a polished fallback with no images',()=>{const f=TestBed.createComponent(PropertyGalleryComponent);f.componentRef.setInput('title','Home');f.detectChanges();expect(f.nativeElement.querySelector('sp-image')).toBeTruthy();expect(f.nativeElement.querySelector('swiper-container')).toBeNull()})});
