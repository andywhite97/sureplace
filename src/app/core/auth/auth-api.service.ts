import {inject,Injectable} from '@angular/core';import {ApiClient} from '../api/api-client';import {AuthTokens,RegistrationResponse,User} from '../models/api.models';
export interface LoginRequest{email:string;password:string} export interface RegisterRequest{first_name:string;last_name:string;email:string;phone_number:string;password:string;onboarding_intents:string[]}
@Injectable({providedIn:'root'}) export class AuthApiService{private api=inject(ApiClient);login(body:LoginRequest){return this.api.post<AuthTokens>('/auth/login/',body)}register(body:RegisterRequest){return this.api.post<RegistrationResponse>('/auth/register/',body)}me(){return this.api.get<User>('/auth/me/')}updateMe(body:Partial<User>){return this.api.patch<User>('/auth/me/',body)}logout(refresh:string){return this.api.post<void>('/auth/logout/',{refresh})}verifyEmail(token:string){return this.api.post<{detail:string;email_verified:boolean}>('/auth/verify-email/',{token})}resendVerification(email:string){return this.api.post<{detail:string}>('/auth/resend-verification/',{email})}changePassword(body:unknown){return this.api.post<{detail:string}>('/auth/change-password/',body)}requestReset(email:string){return this.api.post<{detail:string}>('/auth/password-reset/',{email})}confirmReset(body:unknown){return this.api.post<{detail:string}>('/auth/password-reset/confirm/',body)}}
