import {TestBed} from '@angular/core/testing';import {provideHttpClient} from '@angular/common/http';import {HttpTestingController,provideHttpClientTesting} from '@angular/common/http/testing';import {PropertiesApiService} from './properties-api.service';
describe('PropertiesApiService',()=>{beforeEach(()=>TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]}));it('sends typed filters, sorting, pagination and bounds to v1',()=>{const api=TestBed.inject(PropertiesApiService),http=TestBed.inject(HttpTestingController);api.search({listing_type:'RENT',town:'Ezulwini',min_price:'4000',min_bedrooms:'3',amenities:['1','2'],ordering:'price_asc',page:2,north:'-26',south:'-27',east:'32',west:'31',view:'map'}).subscribe();const request=http.expectOne(r=>r.url==='/api/v1/properties/');expect(request.request.params.get('listing_type')).toBe('RENT');expect(request.request.params.get('amenities')).toBe('1');expect(request.request.params.getAll('amenities')).toEqual(['1','2']);expect(request.request.params.get('ordering')).toBe('price_asc');expect(request.request.params.get('page')).toBe('2');expect(request.request.params.has('view')).toBe(false);request.flush({count:0,next:null,previous:null,results:[]});http.verify()})});
