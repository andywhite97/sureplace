import {Injectable} from '@angular/core';import {ParamMap,Params} from '@angular/router';import {PropertyOrdering,PropertySearchParams,PropertyView} from '../models/property-search.models';
const keys=['listing_type','property_type','region','town','suburb','min_price','max_price','min_bedrooms','min_bathrooms','verification_status','search','north','south','east','west'] as const;
@Injectable({providedIn:'root'}) export class PropertyQueryService{parse(map:ParamMap):PropertySearchParams{const out:PropertySearchParams={ordering:(map.get('ordering') as PropertyOrdering)||'newest',view:(map.get('view') as PropertyView)||'list',page:Math.max(1,Number(map.get('page'))||1)};for(const key of keys){const value=map.get(key);if(value)(out as Record<string,unknown>)[key]=value}for(const key of ['furnished','pet_friendly','featured'] as const)if(map.get(key)==='true')out[key]=true;const amenities=map.getAll('amenities').flatMap(x=>x.split(',')).filter(Boolean);if(amenities.length)out.amenities=amenities;return out}serialize(value:PropertySearchParams):Params{const out:Params={};for(const [key,item] of Object.entries(value)){if(item===undefined||item===''||item===false||(key==='page'&&item===1)||(key==='ordering'&&item==='newest')||(key==='view'&&item==='list'))continue;out[key]=Array.isArray(item)?item.join(','):item}return out}withoutBounds(value:PropertySearchParams){const {north,south,east,west,...rest}=value;return rest}}
