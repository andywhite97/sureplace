import {Component,inject} from '@angular/core';import {ToastService} from '../core/services/toast.service';@Component({selector:'sp-toast-region',standalone:true,template:`<aside aria-live="polite">@for(toast of service.items();track toast.id){<div [class]="toast.kind">{{toast.message}}<button type="button" aria-label="Dismiss" (click)="service.dismiss(toast.id)">×</button></div>}</aside>`,styles:[`aside{position:fixed;z-index:50;right:1rem;top:5rem;display:grid;gap:.6rem}.success{border-left:4px solid var(--teal)}.error{border-left:4px solid var(--danger)}.warning{border-left:4px solid var(--amber)}div{min-width:260px;background:white;padding:1rem;box-shadow:var(--shadow);border-radius:var(--radius-sm)}button{float:right;border:0;background:none}`]})export class ToastRegionComponent{service=inject(ToastService)}
