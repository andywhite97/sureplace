import {Component,DestroyRef,HostListener,inject} from '@angular/core';import {RouterLink,RouterLinkActive,RouterOutlet} from '@angular/router';import {AccountActivityStore} from '../core/services/account-activity.store';import {SeoService} from '../core/services/seo.service';
type AccountBadge='saved'|'messages'|'alerts'|'bookings'|'notifications';type AccountLink={label:string;path:string;badge?:AccountBadge};
@Component({standalone:true,imports:[RouterLink,RouterLinkActive,RouterOutlet],template:`<main class="account"><aside><h1>My SurePlace</h1><nav aria-label="Account navigation"><p>My Account</p>@for(link of links;track link.path){<a [routerLink]="link.path" routerLinkActive="active" [routerLinkActiveOptions]="{exact:link.path==='/account'}"><span>{{link.label}}</span>@if(badge(link.badge)){<b>{{badge(link.badge)}}</b>}</a>}<p>Manage Listings</p>@for(link of manageLinks;track link.path){<a [routerLink]="link.path" routerLinkActive="active"><span>{{link.label}}</span>@if(badge(link.badge)){<b>{{badge(link.badge)}}</b>}</a>}</nav></aside><section class="content"><router-outlet/></section></main>`,styles:[`.account{width:min(1280px,calc(100% - 2rem));margin:2rem auto;display:grid;grid-template-columns:230px 1fr;gap:1.5rem}aside{background:white;padding:1.25rem;border-radius:var(--radius);box-shadow:var(--shadow);align-self:start}h1{font-size:1.25rem}nav{display:grid;gap:.2rem}nav p{margin:.75rem 0 .2rem;color:var(--slate);font-size:.72rem;text-transform:uppercase;font-weight:900}a{display:flex;justify-content:space-between;gap:.7rem;align-items:center;padding:.7rem;border-radius:var(--radius-sm);color:var(--slate);text-decoration:none}.active{background:var(--mist);color:var(--teal);font-weight:700}b{min-width:1.45rem;text-align:center;border-radius:999px;background:var(--teal);color:#fff;font-size:.7rem;padding:.15rem .35rem}.content{min-width:0;background:white;padding:1.5rem;border-radius:var(--radius);box-shadow:var(--shadow)}@media(max-width:760px){.account{display:block;margin:1rem auto 6rem}aside{overflow:auto;margin-bottom:1rem;padding:.7rem}aside h1{display:none}nav{display:flex;width:max-content}nav p{align-self:center;margin:0 .4rem}.content{padding:1rem}}`]})export class AccountShellComponent{store=inject(AccountActivityStore);private seo=inject(SeoService);private destroy=inject(DestroyRef);links:AccountLink[]=[{label:'Overview',path:'/account'},{label:'Saved',path:'/account/saved',badge:'saved'},{label:'Messages',path:'/account/messages',badge:'messages'},{label:'Alerts',path:'/account/alerts',badge:'alerts'},{label:'Viewings',path:'/account/viewings'},{label:'Bookings',path:'/account/bookings',badge:'bookings'},{label:'Notifications',path:'/account/notifications',badge:'notifications'},{label:'Profile',path:'/account/profile'},{label:'Settings',path:'/account/settings'}];manageLinks:AccountLink[]=[{label:'Dashboard',path:'/account/manage'},{label:'Properties',path:'/account/manage/properties'},{label:'Stays',path:'/account/manage/stays'},{label:'Viewings',path:'/account/manage/viewings'},{label:'Bookings',path:'/account/manage/bookings',badge:'bookings'},{label:'Verification',path:'/account/manage/verification'}];constructor(){this.seo.privatePage('My SurePlace','Manage saved listings, messages, bookings and account settings.');this.store.refresh();this.store.startPolling();this.destroy.onDestroy(()=>this.store.stopPolling())}badge(kind?:AccountBadge){const s=this.store.summary();if(!kind||!s)return '';const n=kind==='saved'?this.store.savedTotal():kind==='messages'?this.store.unreadMessages():kind==='alerts'?s.active_saved_searches:kind==='bookings'?s.pending_bookings:kind==='notifications'?s.unread_notifications:0;return n>99?'99+':n?String(n):''}@HostListener('document:visibilitychange')visible(){if(!document.hidden)this.store.refresh(true)}}
