import {Component,input,output} from '@angular/core';@Component({selector:'sp-feedback',standalone:true,template:`<section><h2>{{title()}}</h2><p>{{message()}}</p>@if(requestId()){<small>Support reference: {{requestId()}}</small>}@if(actionLabel()){<button type="button" (click)="action.emit()">{{actionLabel()}}</button>}</section>`,styles:[`section{text-align:center;padding:var(--space-8);background:white;border-radius:var(--radius);box-shadow:var(--shadow)}h2{margin-top:0}button{margin-top:1rem}`]})export class FeedbackComponent{title=input.required<string>();message=input.required<string>();requestId=input<string|null>();actionLabel=input('');action=output<void>()}
