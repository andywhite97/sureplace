import {TestBed} from '@angular/core/testing';import {VerificationBadgeComponent} from './verification-badge.component';describe('VerificationBadgeComponent',()=>{it('uses the backend-provided label',()=>{const fixture=TestBed.createComponent(VerificationBadgeComponent);fixture.componentRef.setInput('label','Verified Agency');fixture.detectChanges();expect(fixture.nativeElement.textContent).toContain('Verified Agency')})});
