import {TestBed} from '@angular/core/testing';import {FormFieldComponent} from './form-field.component';describe('FormFieldComponent',()=>{it('associates and announces validation errors',()=>{const fixture=TestBed.createComponent(FormFieldComponent);fixture.componentRef.setInput('label','Email');fixture.componentRef.setInput('forId','email');fixture.componentRef.setInput('error','Email is required');fixture.detectChanges();expect(fixture.nativeElement.querySelector('label').htmlFor).toBe('email');expect(fixture.nativeElement.querySelector('[role=alert]').textContent).toContain('required')})});
