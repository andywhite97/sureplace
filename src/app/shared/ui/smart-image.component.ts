import {Component,input,signal} from '@angular/core';import {IconComponent} from './icon.component';
@Component({selector:'sp-image',standalone:true,imports:[IconComponent],template:`<div [style.aspect-ratio]="ratio()">@if(src()&&!failed()){<img [src]="src()" [alt]="alt()" [attr.loading]="priority()?'eager':'lazy'" [attr.fetchpriority]="priority()?'high':'auto'" (error)="failed.set(true)">}@else{<sp-icon name="home"/>}</div>`,styles:[`div{display:grid;place-items:center;background:#e7eeec;overflow:hidden}img{width:100%;height:100%;object-fit:cover}sp-icon{font-size:2rem;color:var(--slate)}`]})export class SmartImageComponent{src=input<string|null>();alt=input.required<string>();ratio=input('4 / 3');priority=input(false);failed=signal(false)}
