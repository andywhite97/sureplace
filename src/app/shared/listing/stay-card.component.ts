import {Component,inject,input,OnInit,signal} from '@angular/core';import {Router,RouterLink} from '@angular/router';import {catchError,finalize,of} from 'rxjs';import {StayCard} from '../../core/models/listing.models';import {AuthService} from '../../core/auth/auth.service';import {FavouritesApiService} from '../../core/api/favourites-api.service';import {ToastService} from '../../core/services/toast.service';import {SmartImageComponent} from '../ui/smart-image.component';import {VerificationBadgeComponent} from '../ui/verification-badge.component';import {IconComponent} from '../ui/icon.component';import {formatMoney} from './price-format';
@Component({selector:'sp-stay-card',standalone:true,imports:[RouterLink,SmartImageComponent,VerificationBadgeComponent,IconComponent],template:`<article><div class="visual"><a [routerLink]="['/stays',item().slug]" [attr.aria-label]="'View '+item().name"><sp-image [src]="item().cover_image" [alt]="item().name"/></a><span class="type">{{item().stay_type}}</span><button type="button" class="fav" [class.active]="favourited()" [disabled]="saving()" (click)="toggleFavourite()" [attr.aria-label]="favourited()?'Remove from saved stays':'Save stay'"><sp-icon name="heart"/></button></div><div class="body"><p class="price">@if(price()){From {{price()}} <small>/ night</small>}@else{Price on request}</p><h3><a [routerLink]="['/stays',item().slug]">{{item().name}}</a></h3><p class="location"><sp-icon name="pin"/>{{location()}}</p>@if(item().available_room_type_count){<p class="rooms">{{item().available_room_type_count}} room type{{item().available_room_type_count===1?'':'s'}} available</p>}@if(item().verification_badges[0];as badge){<sp-verification-badge [label]="badge.label"/>}</div></article>`,styleUrl:'./listing-card.scss'})
export class StayCardComponent implements OnInit{item=input.required<StayCard>();private auth=inject(AuthService);private fav=inject(FavouritesApiService);private router=inject(Router);private toast=inject(ToastService);favourited=signal(false);saving=signal(false);ngOnInit(){this.favourited.set(this.item().is_favourited)}price(){return formatMoney(this.item().minimum_nightly_price)}location(){return [this.item().suburb,this.item().town].filter(Boolean).join(', ')}toggleFavourite(){if(!this.auth.isAuthenticated()){void this.router.navigate(['/login'],{queryParams:{returnUrl:`/stays/${this.item().slug}`}});return}const before=this.favourited();this.favourited.set(!before);this.saving.set(true);const action=before?this.fav.remove('stay',this.item().id):this.fav.addStay(this.item().id);action.pipe(catchError(()=>{this.favourited.set(before);this.toast.show('Could not update saved stays.','error');return of(undefined)}),finalize(()=>this.saving.set(false))).subscribe()}}
